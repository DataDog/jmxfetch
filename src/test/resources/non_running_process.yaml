init_config:

instances:
    -   process_name_regex: .*non_running_process_test.*
        default-jmx-metrics-definitions: simplified-default-jmx-metrics.yaml
        name: jmx_test_instance
        tags:
            env: stage
            newTag: test
