init_config:
  is_jmx: true
  collect_default_metrics: false
  collect_default_jvm_metrics: false

instances:
    -   name: jmxint_container
        host: {0}
        collect_default_jvm_metrics: false
        port: {1,number,#}
        conf:
          - include:
              domain: Bohnanza